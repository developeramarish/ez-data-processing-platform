# Hazelcast - StatefulSet
# Week 2: Day 4
# Single-node distributed cache for development (1GB memory)

apiVersion: v1
kind: Service
metadata:
  name: hazelcast
  namespace: ez-platform
  labels:
    app: hazelcast
spec:
  ports:
  - port: 5701
    targetPort: 5701
    name: hazelcast
  clusterIP: None  # Headless service
  selector:
    app: hazelcast

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: hazelcast
  namespace: ez-platform
spec:
  serviceName: hazelcast
  replicas: 1
  selector:
    matchLabels:
      app: hazelcast
  template:
    metadata:
      labels:
        app: hazelcast
        component: cache
    spec:
      containers:
      - name: hazelcast
        image: hazelcast/hazelcast:5.6
        ports:
        - containerPort: 5701
          name: hazelcast
        env:
        - name: JAVA_OPTS
          value: "-Xms256m -Xmx512m"
        - name: HZ_CLUSTERNAME
          value: "data-processing-cluster"
        - name: HZ_KUBERNETES_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: HZ_KUBERNETES_SERVICE_NAME
          value: "hazelcast"
        resources:
          requests:
            cpu: "100m"
            memory: "512Mi"
          limits:
            cpu: "500m"
            memory: "768Mi"
        volumeMounts:
        - name: hazelcast-data
          mountPath: /data
        livenessProbe:
          httpGet:
            path: /hazelcast/health/node-state
            port: 5701
          initialDelaySeconds: 60
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /hazelcast/health/ready
            port: 5701
          initialDelaySeconds: 30
          periodSeconds: 5
  volumeClaimTemplates:
  - metadata:
      name: hazelcast-data
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 5Gi
